{
  "name": "commerce-server",
  "version": "1.0.0",
  "description": "Backend for Commerce App",
  "main": "index.js",
  "repository": "git@github.com:lexyblazy/commerce-server.git",
  "author": "adekola.olalekan01@gmail.com",
  "license": "MIT",
  "husky": {
    "hooks": {
      "pre-commit": "yarn tslint && yarn build"
    }
  },
  "scripts": {
    "build": "rm -rf ./build && yarn tsc",
    "start": "yarn build && node build/index.js",
    "dev": "yarn build && yarn nodemon build/index.js",
    "tslint": "tslint -c tslint.json 'src/**/*.ts'",
    "tslintfix": "tslint -c tslint.json 'src/**/*.ts' --fix"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.8",
    "husky": "^4.3.0",
    "nodemon": "^2.0.4",
    "prettier": "^2.1.2",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "tslint-eslint-rules": "^5.4.0",
    "tslint-plugin-prettier": "^2.3.0",
    "typescript": "^4.0.3"
  }
}
